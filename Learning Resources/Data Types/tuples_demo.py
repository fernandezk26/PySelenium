#tuples are like lists, but they are immutable, and start and end with parentices
#tuple info is accessed just like a list, with indexing
from filecmp import cmp


tuple_a = (1,2,3,4)
tuple_b = (1, 'x')
tuple_c = ('xy',)

#functions
len(tuple_a)

#tuple(list) converts list into tuple

#max(tuple_a) gives the maximum value in tuple_a